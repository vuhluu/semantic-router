version: v0.3

listeners:
  - name: http-8899
    address: 0.0.0.0
    port: 8899
    timeout: 1800s

global:
  router:
    auto_model_names:
      - vllm-sr/auto
  integrations:
    looper:
      endpoint: http://vllm-sr-envoy-container:8899/v1/chat/completions
      timeout_seconds: 1800

providers:
  defaults:
    model: gemini31-worker
    reasoning_effort: high
  models:
    - name: opus48-worker
      provider_model_id: anthropic/claude-opus-4.8
      api_format: openai
      reasoning:
        family: openrouter
      pricing:
        currency: USD
        prompt_per_1m: 15
        cached_input_per_1m: 0
        completion_per_1m: 75
      backend_refs:
        - name: openrouter
          base_url: https://openrouter.ai/api/v1
          provider: openai
          auth_header: Authorization
          auth_prefix: Bearer
          api_key_env: OPENROUTER_API_KEY
          weight: 1
    - name: gemini31-worker
      provider_model_id: google/gemini-3.1-pro-preview
      api_format: openai
      reasoning:
        family: openrouter
      pricing:
        currency: USD
        prompt_per_1m: 2
        cached_input_per_1m: 0
        completion_per_1m: 12
      backend_refs:
        - name: openrouter
          base_url: https://openrouter.ai/api/v1
          provider: openai
          auth_header: Authorization
          auth_prefix: Bearer
          api_key_env: OPENROUTER_API_KEY
          weight: 1

routing:
  modelCards:
    - name: opus48-worker
      context_window_size: 1000000
      description: Closed-model HLE worker for high-confidence reasoning and disagreement checks.
      capabilities: [chat, reasoning, long-context]
      evaluations:
        - benchmark: vllm-sr/operator-rating@1.0.0
          metrics:
            score: 0.95
      modality: text
      tags: [deployment:openrouter, benchmark:hle, role:worker, pool:closed]
    - name: gemini31-worker
      context_window_size: 1000000
      description: Closed-model HLE worker and synthesis/judge model.
      capabilities: [chat, reasoning, long-context]
      evaluations:
        - benchmark: vllm-sr/operator-rating@1.0.0
          metrics:
            score: 0.94
      modality: text
      tags: [deployment:openrouter, benchmark:hle, role:synthesis, pool:closed]

  signals:
    keywords:
      - name: hle_any
        operator: OR
        keywords:
          - "Humanity's Last Exam"
          - "Question:"
          - "Answer:"
          - "Exact Answer:"
          - "Confidence:"
          - "Your response should be in the following format"
      - name: hle_formal_reasoning
        operator: OR
        keywords:
          - "prove"
          - "theorem"
          - "derive"
          - "calculate"
          - "equation"
          - "integral"
          - "probability"
          - "matrix"
          - "physics"
          - "chemistry"
          - "biology"
          - "genetics"
          - "law"
          - "philosophy"
      - name: hle_disagreement_risk
        operator: OR
        keywords:
          - "which of the following"
          - "select all"
          - "except"
          - "least likely"
          - "counterexample"
          - "cannot be determined"
          - "estimate"
          - "approximate"
      - name: hle_exact_answer_contract
        operator: OR
        keywords:
          - "Explanation:"
          - "Exact Answer:"
          - "Confidence:"
          - "succinct, final answer"
    context:
      - name: hle_long_prompt
        min_tokens: 4K
        max_tokens: 1M
        description: Long HLE prompt or reference-heavy question.

  projections:
    scores:
      - name: hle_closed_risk_score
        method: weighted_sum
        inputs:
          - type: keyword
            name: hle_formal_reasoning
            weight: 0.35
          - type: keyword
            name: hle_disagreement_risk
            weight: 0.25
          - type: keyword
            name: hle_exact_answer_contract
            weight: 0.20
          - type: context
            name: hle_long_prompt
            weight: 0.20
    mappings:
      - name: hle_closed_risk_band
        source: hle_closed_risk_score
        method: threshold_bands
        outputs:
          - name: hle_low_risk
            lt: 0.30
          - name: hle_medium_risk
            gte: 0.30
            lt: 0.60
          - name: hle_high_risk
            gte: 0.60

  decisions:
    - name: hle_high_risk_closed_remom
      description: High-risk HLE uses repeated Opus/Gemini closed-model deliberation and Gemini synthesis.
      priority: 340
      rules:
        operator: AND
        conditions:
          - type: projection
            name: hle_high_risk
      output_contract: >-
        Return exactly: Explanation: {concise reasoning}
        Exact Answer: {succinct final answer}
        Confidence: {0%-100% confidence}. Do not add extra sections.
      modelRefs:
        - model: opus48-worker
          use_reasoning: true
          reasoning_effort: high
        - model: gemini31-worker
          use_reasoning: true
          reasoning_effort: high
      algorithm:
        type: remom
        remom:
          breadth_schedule: [4]
          model_distribution: round_robin
          temperature: 0.8
          include_reasoning: false
          compaction_strategy: last_n_tokens
          compaction_tokens: 12000
          synthesis_model: gemini31-worker
          max_concurrent: 4
          round_timeout_seconds: 900
          min_successful_responses: 2
          max_responses_per_round: 4
          include_intermediate_responses: false
          on_error: skip
          synthesis_template: |-
            You are the final HLE answer synthesizer for a closed-model router.
            The candidate panel contains only Claude Opus 4.8 and Gemini 3.1 Pro
            attempts.

            Original question:
            {{.OriginalContent}}

            Candidate attempts:
            {{range $i, $resp := .ReferenceResponses}}
            Attempt {{add $i 1}}{{if $resp.Model}} ({{$resp.Model}}){{end}}:
            {{$resp.Content}}
            {{end}}

            Resolve disagreements using the original question. Prefer answers
            with explicit derivation and contract-compliant exact answers.
            Return exactly:
            Explanation: {your concise reasoning}
            Exact Answer: {your succinct final answer}
            Confidence: {integer percentage between 0% and 100%}

    - name: hle_medium_risk_closed_fusion
      description: Medium-risk HLE uses Opus/Gemini analysis and Gemini synthesis.
      priority: 240
      rules:
        operator: OR
        conditions:
          - type: projection
            name: hle_medium_risk
          - type: keyword
            name: hle_disagreement_risk
      output_contract: >-
        Return exactly: Explanation: {concise reasoning}
        Exact Answer: {succinct final answer}
        Confidence: {0%-100% confidence}. Do not add extra sections.
      modelRefs:
        - model: opus48-worker
          use_reasoning: true
          reasoning_effort: high
        - model: gemini31-worker
          use_reasoning: true
          reasoning_effort: high
      algorithm:
        type: fusion
        fusion:
          model: gemini31-worker
          analysis_models: [opus48-worker, gemini31-worker]
          max_concurrent: 2
          max_completion_tokens: 32768
          round_timeout_seconds: 900
          min_successful_responses: 2
          temperature: 0.8
          include_analysis: false
          include_intermediate_responses: false
          on_error: skip
          synthesis_template: |-
            You are the final HLE answer judge for a closed-model router.

            Original question:
            {{original}}

            Candidate responses:
            {{responses}}

            Select or repair the answer most likely to be judged correct.
            Preserve the required HLE format exactly:
            Explanation: {your concise reasoning}
            Exact Answer: {your succinct final answer}
            Confidence: {integer percentage between 0% and 100%}
          grounding:
            enabled: false
            reference: panel
            policy: weight
            min_score: 0.0
            min_keep: 1
            nli_contradiction_penalty: 1.0
            on_error: skip

    - name: hle_fallback_closed_remom
      description: Catch-all HLE fallback using only Opus and Gemini closed-model attempts.
      priority: 1
      rules:
        operator: AND
        conditions: []
      output_contract: >-
        Preserve the HLE output contract exactly. Return Explanation, Exact
        Answer, and Confidence only.
      modelRefs:
        - model: opus48-worker
          use_reasoning: true
          reasoning_effort: high
        - model: gemini31-worker
          use_reasoning: true
          reasoning_effort: high
      algorithm:
        type: remom
        remom:
          breadth_schedule: [3]
          model_distribution: round_robin
          temperature: 0.8
          include_reasoning: false
          compaction_strategy: last_n_tokens
          compaction_tokens: 8000
          synthesis_model: gemini31-worker
          max_concurrent: 3
          round_timeout_seconds: 900
          min_successful_responses: 2
          max_responses_per_round: 3
          include_intermediate_responses: false
          on_error: skip
          synthesis_template: |-
            Produce the final HLE answer from closed-model attempts only.

            Original question:
            {{.OriginalContent}}

            Attempts:
            {{range $i, $resp := .ReferenceResponses}}
            Attempt {{add $i 1}}{{if $resp.Model}} ({{$resp.Model}}){{end}}:
            {{$resp.Content}}
            {{end}}

            Return exactly:
            Explanation: {your concise reasoning}
            Exact Answer: {your succinct final answer}
            Confidence: {integer percentage between 0% and 100%}
