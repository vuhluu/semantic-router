config:
  version: v0.3
  listeners: []
  providers:
    defaults:
      model: base-model
      reasoning_effort: high
    models:
      - name: base-model
        reasoning:
          family: qwen3
        backend_refs:
          - name: local-vllm
            provider: vllm
            endpoint: vllm-llama3-8b-instruct.default.svc.cluster.local:8000
            weight: 1
  routing:
    decisions:
      # ── Security layer: block before routing (priority 1000+) ──
      # These decisions verify that fast_response blocking works correctly
      # even when the request body arrives in Envoy streamed chunks.
      - name: block_jailbreak
        description: Block jailbreak attempts in streamed body mode
        priority: 1000
        rules:
          operator: OR
          conditions:
            - type: jailbreak
              name: jailbreak_standard
        plugins:
          - type: fast_response
            configuration:
              message: "I'm sorry, but I cannot process this request as it appears to violate our usage policies."
      - name: block_pii
        description: Block requests containing PII in streamed body mode
        priority: 999
        rules:
          operator: OR
          conditions:
            - type: pii
              name: pii_deny_all
        plugins:
          - type: fast_response
            configuration:
              message: "Your request contains personal information that cannot be processed."
      - name: code_keywords_decision
        description: Code and programming queries (BM25 classified)
        priority: 150
        rules:
          operator: AND
          conditions:
            - type: keyword
              name: code_keywords
        modelRefs:
          - model: base-model
            use_reasoning: true
        plugins:
          - type: system_prompt
            configuration:
              system_prompt: You are an expert software engineer.
      - name: urgent_request_decision
        description: Urgent and time-sensitive requests (N-gram classified)
        priority: 150
        rules:
          operator: AND
          conditions:
            - type: keyword
              name: urgent_request
        modelRefs:
          - model: base-model
            use_reasoning: false
      - name: general_decision
        description: Catch-all with semantic cache for all unmatched requests
        priority: 50
        rules:
          operator: AND
          conditions: []
        modelRefs:
          - model: base-model
            use_reasoning: false
        plugins:
          - type: response_cache
            configuration:
              enabled: true
              scope: global
              similarity_threshold: 0.6
    signals:
      keywords:
        - name: code_keywords
          operator: OR
          method: bm25
          keywords:
            - code
            - function
            - implement
            - debug
            - algorithm
            - compile
            - syntax
            - variable
            - loop
            - refactor
          bm25_threshold: 0.1
          case_sensitive: false
        - name: urgent_request
          operator: OR
          method: ngram
          keywords:
            - urgent
            - immediate
            - asap
            - emergency
          ngram_threshold: 0.4
          ngram_arity: 3
          case_sensitive: false
        - name: sensitive_data
          operator: AND
          keywords:
            - SSN
            - credit card
          case_sensitive: false
      domains:
        - name: code_keywords
          description: Programming and software development queries
          mmlu_categories:
            - "computer science"
        - name: urgent_request
          description: Urgent and time-sensitive requests
        - name: sensitive_data
          description: Requests involving sensitive personal data
        - name: business
          description: Business and management related queries
          mmlu_categories:
            - business
        - name: other
          description: General knowledge and miscellaneous topics
          mmlu_categories:
            - other
      jailbreak:
        - name: jailbreak_standard
          threshold: 0.6
          include_history: false
          description: Standard jailbreak detection for streaming tests
      pii:
        - name: pii_deny_all
          threshold: 0.4
          include_history: false
          description: Block all PII
    modelCards:
      - name: base-model
  global:
    model_catalog:
      kbs: []
    router:
      streamed_body:
        enabled: true
        max_bytes: 10485760
        timeout_sec: 30
      strategy: priority
    services:
      api:
        batch_classification:
          max_batch_size: 100
          concurrency_threshold: 5
          max_concurrency: 8
      observability:
        tracing:
          enabled: true
          provider: opentelemetry
          exporter:
            type: otlp
            endpoint: jaeger:4317
            insecure: true
          sampling:
            type: always_on
            rate: 1.0
          resource:
            service_name: vllm-semantic-router
            service_version: v0.1.0
            deployment_environment: development
    stores:
      response_cache:
        enabled: true
        backend_type: memory
        similarity_threshold: 0.6
        max_entries: 1000
        ttl_seconds: 3600
        eviction_policy: fifo
        embedding_model: mmbert
      memory:
        embedding_model: mmbert
      vector_store:
        embedding_model: mmbert
    integrations:
      tools:
        enabled: true
        top_k: 3
        similarity_threshold: 0.2
        tools_db_path: config/tools_db.json
        fallback_to_empty: true
